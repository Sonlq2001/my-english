import { styled } from "styled-components";

export const WrapTextEditor = styled.div`
  label {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    width: fit-content;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    .label-require {
      color: red;
      font-size: 1.8rem;
    }
  }
`;
